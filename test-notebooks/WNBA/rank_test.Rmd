---
title: "WNBA Archetypes"
output:
  html_notebook: 
    theme: readable
  html_document: default
editor_options: 
  chunk_output_type: inline
---

## Libraries
```{r message=FALSE}
# devtools::install(
#   "~/Projects/Mastering-DFS-Analytics/dfstools/",
#   build_vignettes = TRUE
# )
library(dfstools)
library(archetypes)
library(DT)
library(tibble)
library(dplyr)
library(ggtern)
library(readr)
```

## Get data
```{r}
playerbox <- playerboxscore(
    "~/Dropbox/17-wnba-player/wnba-season-player-feed-06-09-2017.xlsx"
)
gamebox <- gameboxscore(playerbox)
```

## Collect model input
```{r}
game_data <- collect_game_data(gamebox, 200, '2017-06-09')
```

## Build model
```{r}
season_model <- build_model(game_data)
```

## Back-predict for a test
```{r}
back_prediction <- game_predict(season_model, game_data)
```

## get the new rows
```{r}
salaries <- read_csv(
  "/home/Projects/draftkings/contest-standings/wnba/20170610/FanDuel-WNBA-2017-06-10-19655-players-list.csv",
  col_types = cols(
    X14 = col_skip(),
    X15 = col_skip()
  )
)
View(salaries)
decoder <- tribble(
  ~fanduel, ~bigdataball,
  "PHO", "Phoenix",
  "LA", "Los Angeles",
  "ATL", "Atlanta",
  "CON", "Connecticut",
  "CHI", "Chicago",
  "SA", "San Antonio"
)
new_rows <- tribble(
  ~away, ~home,
  "Los Angeles", "Phoenix",
  "Atlanta", "Connecticut",
  "Chicago", "San Antonio"
)
View(new_rows)
prediction <- game_predict(season_model, new_rows)
View(prediction)
score_table <- rank_scores(prediction)
View(score_table)
```


